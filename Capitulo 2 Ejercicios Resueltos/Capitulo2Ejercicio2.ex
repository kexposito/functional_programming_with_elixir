defmodule Capitulo2Ejercicio2 do 
    def total_distance(distance, time) do
       IO.puts("Su distancia:  #{distance} kilometros \n") 
       IO.puts("En  #{time} horas \n")
       IO.puts("Su promedio es de:  #{distance/time} kilometros por hora\n") 
    end
end
